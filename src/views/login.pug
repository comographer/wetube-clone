extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST") 
        input(placeholder="Username" name="username" type="text" required)
        input(placeholder="Password" name="password" type="password" required)
        input(type="submit" value="Login")
        br
        a(href="https://github.com/login/oauth/authorize?client_id=c38d339924eff5349509&scope=") Continue with GitHub &rarr;
    hr
    div 
        span Don't have an ccount? 
        a(href="/join") Create now &rarr;